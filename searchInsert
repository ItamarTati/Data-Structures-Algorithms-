var searchInsert = function(nums, target) {
    let answer = nums.indexOf(target)
    if (answer === -1){
        nums.push(target)
        nums.sort((a, b) => a - b)
        return nums.indexOf(target)
    }
    else{
        return answer
    }
};
