var subtractProductAndSum = function(n) {
    let product = n.toString().split('').reduce((acc, curr) => parseInt(acc) * parseInt(curr))
    let sum = n.toString().split('').reduce((acc, curr) => parseInt(acc) + parseInt(curr))
    return product - sum
};
