var flipAndInvertImage = function(A) {
    A.length
    for(let index = 0; index < A.length; index++){
        A[index] = A[index].reverse().map((number) => number === 1 ? 0 : 1)
    }
    return A
};
