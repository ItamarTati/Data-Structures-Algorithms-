function isPalindrome(s: string): boolean {
    let regex = /[\W_]/g
    let lowerCaseRegex: string = s.toLowerCase().replace(regex, '')
    return lowerCaseRegex === s.toLowerCase().replace(regex, '').split('').reverse().join('') 
};
