var plusOne = function(digits) {
    for (let index = digits.length - 1; index >= 0; index--){
        digits[index]++
        if(digits[index] > 9){
            digits[index] = 0
        }else{
            return digits
        }
            
    }
    digits.unshift(1)
    return digits
};
