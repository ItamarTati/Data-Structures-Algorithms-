var heightChecker = function(heights) {
    let newarray = heights.slice(0).sort((a, b) => a - b)
    let count = 0
    for(let index = 0; index < heights.length; index++){
        if (newarray[index] !== heights[index]){
            count++
        }
    }
    return count
    
};
