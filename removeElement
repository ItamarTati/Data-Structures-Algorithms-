var removeElement = function(nums, val) {
    for(let index = 0; index < nums.length; index++){
        if(val === nums[index]){
            nums.splice(index, 1)
            index--
        }
    }
    return nums.length
};
